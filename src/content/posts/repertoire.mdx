---
title: 'レパートリー'
description: 'やまなみコーラスクラブの演奏楽曲一覧'
pubDate: 2025-12-03
---

import { groupedByComposer, sortedComposers } from '@/utils/repertoire'

<div class="history-list">
  {sortedComposers.map((composer) => (
    <div class="composer-group">
      <h3 class="composer-name">{composer}</h3>
      <ul class="song-list">
        {groupedByComposer[composer].map((item) => (
          <li class="song-item">
            <span class="song-title">{item.title}</span>
            <span class="lyricist">（text: {item.lyricist}）</span>
          </li>
        ))}
      </ul>
    </div>
  ))}
</div>

<style>{`
  .history-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    background-color: color-mix(in srgb, var(--card) 92%, transparent);
    border-radius: var(--radius, 10px);
    padding: 0.5rem;
  }

  .composer-group {
    padding: 0.25rem;
    margin-top: 0rem;
    margin-bottom: 0rem;
    background-color: color-mix(in srgb, var(--card) 96%, transparent);
    border-radius: var(--radius, 10px);
  }

  .composer-name {
    font-size: var(--font-size-m);
    margin-bottom: 0.5rem;
    color: var(--text-primary);
    font-weight: var(--font-weight-medium);
  }

  .song-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0rem;
  }

  .song-item {
    font-size: var(--font-size-m);
    color: var(--text-primary);
    padding-left: 1.5rem;
    position: relative;
  }

  .song-item::before {
    content: '・';
    position: absolute;
    left: 0;
  }

  .song-title {
    font-weight: var(--font-weight-light);
  }

  .lyricist {
    font-size: var(--font-size-s);
    color: var(--text-tertiary);
    margin-left: 0.5rem;
  }
`}</style>
